syntax = "proto3";

option java_package = "emu.grasscutter.net.proto";

message CoopReward {
	uint32 id = 13;
	State state = 7;

	enum State {
		STATE_UNLOCK = 0;
		STATE_LOCK = 1;
		STATE_TAKEN = 2;
	}
}
